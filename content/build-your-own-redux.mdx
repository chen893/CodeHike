import { TutorialCodeBlock } from "../components/tutorial-code-block"
import { ReduxScrollySection } from "../components/redux-scrolly-section"
import { finalStoreCode } from "../lib/redux-tutorial-content"

<article className="tutorial-content">
  <ReduxScrollySection />

  <section className="tutorial-summary">
    <h2>What you will build</h2>
    <div>
      这一版教程会实现一个最小可用的 Redux 风格 store。它能做四件事：持有状态、
      读取状态、派发 action，以及在状态变化后通知订阅者。
    </div>
    <ul>
      <li>用 reducer 计算初始 state</li>
      <li>通过 getState 暴露只读访问</li>
      <li>通过 subscribe 注册与取消监听</li>
      <li>通过 dispatch 驱动状态变化并广播更新</li>
    </ul>
  </section>

  <section className="tutorial-summary">
    <h2>The complete store</h2>
    <div>
      当上面的步骤全部走完后，你得到的 store 实现大致如下。它还很小，但已经具备了
      Redux 的核心形状：状态只存在于 store 内部，变化只经由 action 进入 reducer，
      外部系统则通过订阅获得更新。
    </div>
    <TutorialCodeBlock code={finalStoreCode} fileName="store.js" />
  </section>

  <section className="tutorial-summary">
    <h2>What this version intentionally leaves out</h2>
    <div>
      一个真正的 Redux 教程还可以继续往前走，比如 reducer 组合、不可变更新、
      middleware、devtools 集成，甚至 React 绑定。但这些都不应该压在最开始这一步。
    </div>
    <div>
      如果第一版就把所有术语都塞进来，读者学到的通常只是 API 名称，而不是 Redux
      的结构。先把最小 store 做出来，后续每一层能力才有明确的落脚点。
    </div>
  </section>

  <section className="tutorial-summary tutorial-summary-last">
    <h2>Where to go next</h2>
    <div>
      下一步最自然的扩展，是继续补一个小型 counter 示例，让这个 store 真正驱动一个
      DOM 或 React 视图。那时你会更直观地看到 subscribe 和 dispatch 为什么是 Redux
      最重要的两个接口。
    </div>
  </section>
</article>
